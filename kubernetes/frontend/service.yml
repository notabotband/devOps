apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    application: front
spec:
  type: ClusterIP
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    application: front
